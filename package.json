{
  "name": "design-to-ship-verification",
  "private": true,
  "version": "0.5.0",
  "description": "Optional browser verification for Design to Ship examples",
  "scripts": {
    "test": "npm run test:browser",
    "test:browser": "node tests/browser/verify.cjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}

