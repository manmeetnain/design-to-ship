# Flutter implementation pack

Use semantic widgets, theme extensions, constrained responsive composition, and explicit state ownership. Prefer adaptive platform behavior where user expectations differ. Avoid fixed pixel layouts, deeply nested one-off widget trees, and visual-only custom controls.

Map tokens through `ThemeData` and semantic extensions. Verify text scaling, screen-reader semantics, focus traversal, compact and expanded layouts, dark mode, localization, slow operations, and restoration where required.

