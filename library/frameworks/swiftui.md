# SwiftUI implementation pack

Use native semantics, navigation, controls, Dynamic Type, environment values, and accessibility modifiers. Separate reusable visual primitives from feature state. Avoid fixed frames that break text growth, gesture-only actions, and custom controls that discard platform behavior.

Map semantic design tokens into environment-aware values. Verify compact layouts, large Dynamic Type, VoiceOver traversal, dark appearance, reduced motion, permission denial, offline state, and task cancellation.

